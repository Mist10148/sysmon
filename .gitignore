# Nothing here is built, so there is very little to ignore.
# Editor and OS litter
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# Exported data files land in Downloads, but if one is dragged in by mistake
# it should not be committed over the samples in data/.
/*.txt

# Design source exported from Claude Design - 843KB of canvas runtime, not app code.
SysMon Mockups (standalone).html
